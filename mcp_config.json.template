{
  "mcpServers": {
    "warlock": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-e", "GITHUB_TOKEN",
        "ghcr.io/works-by-worrell/warlock-mcp:latest",
        "--transport", "stdio"
      ]
    }
  }
}
